<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
android:layout_height="wrap_content"
    android:layout_marginVertical="7dp"
    android:layout_marginHorizontal="7dp"
android:orientation="vertical"
    android:backgroundTint="@color/blue"
android:background="@drawable/edit_text_blue_border"
android:padding="8dp">>

    <!-- Image -->

    <!-- Price -->

    <!-- Customize Button -->
    <ImageView
        android:id="@+id/coverImageView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@drawable/edit_text_blue_border"
        android:layout_marginHorizontal="10dp"
        android:scaleType="centerCrop"
        app:layout_constraintBottom_toBottomOf="@+id/customizeButton"
        app:layout_constraintEnd_toStartOf="@+id/informationTextView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/informationTextView" />

    <TextView
        android:id="@+id/informationTextView"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:textColor="@android:color/white"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/customizeButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/informationTextView"
        android:layout_marginTop="5dp"
        android:layout_centerHorizontal="true"
        android:backgroundTint="@color/blue"
        android:text="Service Name"
        android:textColor="@android:color/white"
        app:layout_constraintTop_toBottomOf="@+id/informationTextView"
        app:layout_constraintEnd_toEndOf="@+id/informationTextView"
        app:layout_constraintStart_toStartOf="@+id/informationTextView" />

</androidx.constraintlayout.widget.ConstraintLayout>
